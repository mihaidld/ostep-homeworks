Homework (Code)
In this homework, you’ll gain some experience with writing concurrent
code and measuring its performance. Learning to build code that
performs well is a critical skill and thus gaining a little experience here
with it is quite worthwhile.

Questions
1. We’ll start by redoing the measurements within this chapter. Use
the call gettimeofday() to measure time within your program.
How accurate is this timer? What is the smallest interval it can measure? 
Gain confidence in its workings, as we will need it in all subsequent questions. You can also look into other timers, such as the
cycle counter available on x86 via the rdtsc instruction.

 struct timeval {
    time_t      tv_sec;     // seconds
    suseconds_t tv_usec;    // microseconds
 }

Smallest interval to measure is microseconds (1 microsecond = 1000 nanoseconds)

2. Now, build a simple concurrent counter and measure how long it
takes to increment the counter many times as the number of threads
increases. How many CPUs are available on the system you are
using? Does this number impact your measurements at all?

2,6 GHz 6-Core Intel Core i7
$ sysctl -n hw.ncpu
12

before: 0
after: 2000000
84237 microseconds for 2 threads, each incrementing 1000000

before: 0
after: 4000000
189263 microseconds for 4 threads, each incrementing 1000000

3. Next, build a version of the approximate counter. 
Once again, measure its performance as the number of threads varies, 
as well as the threshold. 
Do the numbers match what you see in the chapter?

Threshold 10000
136 microseconds for 4 threads, each incrementing 1000000

Threshold 1024
165 microseconds for 4 threads, each incrementing 1000000

Much better performance even with lower threshold

4. Build a version of a linked list that uses hand-over-hand locking
[MS04], as cited in the chapter. You should read the paper first
to understand how it works, and then implement it. Measure its
performance. When does a hand-over-hand list work better than a
standard list as shown in the chapter?

Using regular list:
return code of lookup 2 in list of 1's -1
return code of lookup 2 in list of 1's -1
return code of lookup 2 in list of 1's -1
return code of lookup 2 in list of 1's -1
660613 microseconds for 4 threads, each adding 1000000 1's, at the end checking for 2 inside list

Using hand-over-hand list:
203995 microseconds for 4 threads, each adding 1000000 1's, at the end checking for 2 inside list

Since there is no lock unlock in insert (new node), but only in lookup
this list is faster than regular list when many inserts and few lookups.
Regular list is better for few inserts and many lookups.

5. Pick your favorite data structure, such as a B-tree or other slightly
more interesting structure. Implement it, and start with a simple
locking strategy such as a single lock. Measure its performance as
the number of concurrent threads increases.

Using hash table with simple list for each bucket:
return code of lookup 1000000 in hash table of 1's -1
return code of lookup 1000000 in hash table of 1's -1
return code of lookup 1000000 in hash table of 1's -1
return code of lookup 1000000 in hash table of 1's -1
94283 microseconds for 4 threads, each adding 1000000 numbers 
in [0, 1000000), at the end checking for 1000000 inside hash table

6. Finally, think of a more interesting locking strategy for this favorite
data structure of yours. Implement it, and measure its performance.
How does it compare to the straightforward locking approach?

Using hash table with hand-over-hand list for each bucket:
return code of lookup 1000000 in hash table of 1's -1
return code of lookup 1000000 in hash table of 1's -1
return code of lookup 1000000 in hash table of 1's -1
return code of lookup 1000000 in hash table of 1's -1
110979 microseconds for 4 threads, each adding 1000000 numbers in [0, 1000000), at the end checking for 1000000 inside hash table